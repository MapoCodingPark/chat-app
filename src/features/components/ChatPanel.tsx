import styled from '@emotion/styled';

const ChatPanel = () => {
  return <ChatPanelWrapper>hi</ChatPanelWrapper>;
};

export default ChatPanel;

const ChatPanelWrapper = styled.div`
  flex: 1 1 0;
  height: 100%;
  background-color: yellow;
`;
